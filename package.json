{
  "name": "lerna_boilerplate",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/rahuljain840/lerna_boilerplate.git"
  },
  "devDependencies": {
    "jest": "^26.0.1",
    "lerna": "^3.22.1"
  },
  "scripts": {
    "prepublish": "yarn build",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run --parallel --scope \"@rahuljain840/*\" build",
    "test": "jest"
  }
}
